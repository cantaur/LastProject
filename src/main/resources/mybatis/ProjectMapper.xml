<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.pium.mapper.ProjectMapper">

    <select id="list" resultType="ProjectDTO">
        SELECT * FROM project
    </select>

    <insert id="insert" parameterType="ProjectDTO">
        INSERT INTO project (project_title, project_content, project_status, project_isdelete,  member_seq) VALUES(#{project_title},#{project_content},"0","0",)

    </insert>



</mapper>